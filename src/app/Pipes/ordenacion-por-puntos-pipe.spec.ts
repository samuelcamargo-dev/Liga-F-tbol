import { OrderByPointsPipe } from './ordenacion-por-puntos-pipe';

describe('OrdenacionPorPuntosPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderByPointsPipe();
    expect(pipe).toBeTruthy();
  });
});
